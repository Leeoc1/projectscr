# # 마리아DB 연결
# spring.application.name=thescreen
# spring.datasource.url=jdbc:mariadb://localhost:3306/theater_db 
# spring.datasource.username=root 

# # 마리아DB 비밀번호
# spring.datasource.password=your_password 

# spring.datasource.driver-class-name=org.mariadb.jdbc.Driver 
# spring.jpa.hibernate.ddl-auto=update 
# spring.jpa.show-sql=true 
# spring.jpa.properties.hibernate.format_sql=true 
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
# server.port=8080
# server.servlet.context-path=/api 

# H2 데이터베이스 (최소 설정)
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=

# JPA 설정 (최소 설정)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# 타임리프 설정
spring.thymeleaf.cache=false
# spring.thymeleaf.prefix=classpath:/templates/  # 기본값과 동일
# spring.thymeleaf.suffix=.html                  # 기본값과 동일

# 서버 설정
# server.port=8080
# server.servlet.context-path=/api  # H2 콘솔을 위해 주석 처리